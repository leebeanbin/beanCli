{
  "name": "@tfsdc/policy",
  "version": "0.1.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tfsdc/domain": "workspace:*",
    "@tfsdc/kernel": "workspace:*"
  }
}
