/**
 * beanCLI — Ink TUI entry point (Phase 0+)
 *
 * Run:  pnpm dev:cli:ink
 *
 * Uses @tfsdc/tui (Ink-based). The original index.ts (custom renderer)
 * remains intact and can still be run with: pnpm dev:cli
 */
import { start } from '@tfsdc/tui';

start();
